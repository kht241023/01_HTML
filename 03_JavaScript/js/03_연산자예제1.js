// 1. 문서 내에 input1 input2 input3 calcResult 요소를 가져오는 const 설정

const number1 = document.getElementById("input1"); //input1
const number2 = ; //input2
const number3 = ; //input3
const result = ; //calcResult

// 각 number1 = input1      number2 = input2    number3 = input3
//     value(값)을 가지고 오기

const value1 = number1.value;
const value2 = number2.값;
const value3 = number3.값;

function 더하기기능(){
    result.innerText = Number(value1) + 숫자(    ) + 숫자(    );
}

function 곱하기기능(){
    결과.안에텍스트 = 숫자(    ) * 숫자(    ) * 숫자(    );
}
